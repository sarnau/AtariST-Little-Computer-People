/*
 * astubs.c -- placeholder for doAct() handlers not yet ported.
 *
 * Empty as of the current tree: every AI action + delivery event has
 * a real body in its per-group action_*.c file.  Kept so the linker
 * still picks up a translation unit at this path if a stub is later
 * reintroduced during a port-in-progress.
 */
